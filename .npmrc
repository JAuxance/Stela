# Don't re-verify/reinstall deps before every `pnpm run` — esbuild's build
# script is intentionally not run (its binary ships via a platform optional
# dependency), and that "ignored build" must not block `pnpm build` in CI.
verify-deps-before-run=false
